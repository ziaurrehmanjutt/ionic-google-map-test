import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zia.maptest',
  appName: 'mapTest',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
